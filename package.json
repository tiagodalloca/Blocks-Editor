{
		"name": "blocks-editor",
		"version": "0.1.0",
		"main": "app/main.js",
		
		"devDependencies": {			
				"electron": "^1.6.2",
				"closurecompiler-externs": "^1.0.4"
		},

		"scripts": {
				"build": "electron-packager app --all  --asar true --icon app/assets/icon-c --out app/release-builds --overwrite", 
				"start": "electron ./app"}
}

